var ShopMenu = new Phaser.Class({

    Extends: Phaser.Scene,

    initialize:

    function ShopMenu ()
    {
        Phaser.Scene.call(this);
    },

    create: function () 
    {
        console.log('ShopMenu');
  
    }

});
