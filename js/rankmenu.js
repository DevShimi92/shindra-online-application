var RankMenu = new Phaser.Class({

    Extends: Phaser.Scene,

    initialize:

    function RankMenu ()
    {
        Phaser.Scene.call(this);
    },

    create: function () 
    {
        console.log('RankMenu');
  
    }

});
