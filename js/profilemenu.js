var ProfileMenu = new Phaser.Class({

    Extends: Phaser.Scene,

    initialize:

    function ProfileMenu ()
    {
        Phaser.Scene.call(this);
    },

    create: function () 
    {
        console.log('ProfileMenu');
  
    }

});
