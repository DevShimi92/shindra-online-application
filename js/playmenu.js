var PlayMenu = new Phaser.Class({

    Extends: Phaser.Scene,

    initialize:

    function PlayMenu ()
    {
        Phaser.Scene.call(this);
    },

    create: function () 
    {
        console.log('PlayMenu');
  
    }

});
