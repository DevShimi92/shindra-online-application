var OptionMenu = new Phaser.Class({

    Extends: Phaser.Scene,

    initialize:

    function OptionMenu ()
    {
        Phaser.Scene.call(this);
    },

    create: function () 
    {
        console.log('OptionMenu');
  
    }

});
